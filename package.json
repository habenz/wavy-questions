{
  "name": "wavy-questions",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=out.js --loader:.html=text --serve=9000 --servedir=. --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.23.0"
  }
}
